from django.urls import path
from . import views

urlpatterns = [
    # path('start/', views.start_simulation, name='start_simulation'),
    # path('state/', views.get_state, name='get_simulation_state'),
]
